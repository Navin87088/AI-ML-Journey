#LIST
m1 = 90
m2 = 86
m3 = 95
m4 = 83

marks = [90, 86, 95, 83, 70]
print(marks)


type(a)
o/p = list

type(marks)
O/P  --> list


# Empty List

student = []
print(students)

what is the purpose of storing the data
Ans. so we can use it later

#Indexing

marks[0]
O/P = 90

n= len(marks)
print(n)
O/P = 5

#to access the last marks we can also use
marks[n-1]


# Python has something unique which is called -ve indexing

marks[-2]

marks[5]
O/p -->
error if only 0-4 index

this are on in one containers
 to change the marks you can simply do

marks[2] = 87

by doing this marks will automatically update

#lterating over a list

n = len(marks)
print(n)

O/p --> 5

for i in range(n):
  print(marks[i])

for elem in marks:
  print(elem)

# Write a code to print the elements presents at even indexes

for elem in marks:
  if elem % 2 == 0:
    print(elem)

# indexes which are even 0, 2, 4.
for i in range(n):
  if i%2 == 0:
    print(marks[i])

#even elements
for i in range(n):
  if marks[i]%2 == 0:
    print(marks[i])


# What is printed?

numbers =[5,15,10]

for i in range(len(numbers)):
  print(numbers[i])


# Write a code to find the sum of all the elements of the list.

marks = [ 60, 70, 90, 80 ]

sum = 0
for elem in marks:
  sum = sum + elem

print(sum)

# write a code to find the average of all the elements of the list.
# sum(marks) is not working because the variable is already used 

marks = [ 60, 70, 90, 80 ]

sum = 0
for elem in marks:
  sum = sum + elem

print(sum/len(marks))


# What is printed ?

numbers = [9,5,7,1]

print(sorted(numbers))

# for permanent sort number important
numbers.sort()
print(numbers) 


nums = [2,4,6,8]

count = 0

for n in nums:
  if n > 4:
    count += 1
print(count)


numbers = [3,5,7]

result = 1

for n in numbers:
  result = result * n

print(result)


